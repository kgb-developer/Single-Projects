// Exercise Test
var convert     = require('../exercise_1.js'),
    assert      = require('assert');

describe('Returns 3', function() {
    describe('remoteMathService', function() {
        it('should convert if ANSWER equals 3', function() {
            assert.equal(); // Not sure what to do after this point -- this is where i'd sink some time into learning something new
        });
    });
  });